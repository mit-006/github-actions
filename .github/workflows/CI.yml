#this is calling other workflows

name: ci
      
on: 
    push

jobs:
    code-lint:
        uses: ./.github/workflows/test_and_lint.yml
      
    build-and-push:
        needs: code-lint
        uses: ./.github/workflows/docker.yml
        secrets: inherit 
